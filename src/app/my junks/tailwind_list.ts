// Background Colors
// bg-white → white background in light mode
// dark:bg-gray-900 → dark gray background in dark mode

// Border
// border-gray-200 → light gray border in light mode
// dark:border-gray-700 → darker gray border in dark mode

// Sizing & Layout
// max-w-screen-xl → max width at 1280px
// mx-auto → horizontally centers container
// p-4 → 1rem padding all around

// Flexbox
// flex → enables flex layout
// flex-wrap → allows items to wrap
// items-center → vertically centers items
// justify-between → spaces items evenly with space in between

// Flex Layout
// flex → horizontal flex container
// items-center → vertical centering

// Spacing
// space-x-3 → horizontal gap between child elements
// rtl:space-x-reverse → reverses spacing for right-to-left layouts

// Layout
// inline-flex → flex container inline
// items-center → vertical centering
// justify-center → horizontal centering

// Sizing
// p-2 → padding 0.5rem
// w-10 h-10 → fixed size 2.5rem each

// Typography
// text-sm → small font size

// Colors
// text-gray-500 → gray text in light mode
// dark:text-gray-400 → gray text in dark mode
// hover:bg-gray-100 → light background hover
// dark:hover:bg-gray-700 → dark background hover in dark mode

// Borders & Shape
// rounded-lg → rounded corners

// Visibility
// hidden → hidden by default
// md:hidden → hidden on medium+ screens
// md:block → shown as block from medium screens up

// Focus Styling
// focus:outline-none → removes default outline
// focus:ring-2 focus:ring-gray-200 → focus ring in light mode
// dark:focus:ring-gray-600 → focus ring in dark mode

// Width
// w-full → full width on small screens
// md:w-auto → automatic width on medium+ screens

// Navbar List

//Layout

// flex flex-col → vertical layout
// md:flex-row → horizontal layout on medium+ screens

// Spacing
// p-4 → padding on small screens
// md:p-0 → no padding on medium+
// mt-4 → margin top on small
// md:mt-0 → no top margin on medium+
// md:space-x-8 → horizontal space between items on medium+
// rtl:space-x-reverse → reverse space for RTL

// Styling
// font-medium → medium weight text
// border border-gray-100 → light border
// rounded-lg → rounded corners
// bg-gray-50 → light gray background
// md:bg-white → white background medium+
// dark:bg-gray-800 → dark gray in dark mode
// md:dark:bg-gray-900 → darker background medium+ dark mode
// dark:border-gray-700 → dark border in dark mode
// md:border-0 → no border medium+
